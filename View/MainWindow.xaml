<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="Contacts" Height="410" Width="470">
    <Grid Margin="15,15,15,15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Edit Contact" Grid.Column="0" Margin="0 0 3 0">
            <StackPanel>
                <TextBlock Text="Name:"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0 3 0 3"/>

                <TextBlock Text="Phone Number:"/>
                <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0 3 0 3"/>

                <TextBlock Text="Email:"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0 3 0 3"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Read Contact" Grid.Column="1" Margin="3 0 0 0">
            <StackPanel>
                <Label Content="Name:"/>
                <Label Content="{Binding Name}" FontWeight="Bold"/>
                
                <Label Content="Phone number:"/>
                <Label Content="{Binding PhoneNumber}" FontWeight="Bold"/>
                
                <Label Content="Email:"/>
                <Label Content="{Binding Email}" FontWeight="Bold"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 10 0 0">
            <Button Content="Save" Width="75" Height="25" Command="{Binding SaveCommand}" Margin="0 0 3 0"/>
            <Button Content="Load" Width="75" Height="25" Command="{Binding LoadCommand}"/>
        </StackPanel>
    </Grid>
</Window>
